## TASKS LOG
## 2026-04-17
1. Decolonization key cleanup and description polish
- Completed: replaced all per-country `ztr_subject_created_*` flags in the regenerated decolonization events with the shared `ztr_decolonization_subject_cd` variable, swapped all option C and option E event names to the shared localization keys `ztr_decolonization.c` and `ztr_decolonization.e`, and refreshed the new phase descriptions with stronger visual structure.
- Updated: `events/decolonization/ztr_decolonization.txt`, `localization/english/ztr_decolonization_events_l_english.yml`, `scripts/generate_decolonization.ps1`
- Verified: the generated event file now saves `ztr_decolonization_subject_cd`, no longer references `ztr_decolonization.N.c` or `ztr_decolonization.N.e`, standard/special events that use culture-aware localization now save `ztr_decolonization_culture_scope` in `immediate`, and India keeps a bespoke country-focused description prefix instead of a misleading single-culture scope.

## 2026-04-18
3. Replace dp_independence with dp_annex_war in .e options
- Completed: changed all 65 War-of-Independence option E blocks across events 2-66 so that ROOT now creates `dp_annex_war` targeting the decolonizing country instead of the decolonizing country creating `dp_independence` against ROOT; also removed the `create_diplomatic_pact = colony` line from every option E. `add_journal_entry` (je_demographic_0_to_1) lines were preserved. Special cases: Trucial States (two colony pacts both removed, annex war vs c:BHN), India (scope:ztr_indian_unifier_scope reference kept), Ethiopia (partially-changed wrong-scope block corrected). Event 100 dp_independence left untouched.
- Updated: `events/decolonization/ztr_decolonization.txt`
- Verified: 1 remaining `dp_independence` is event 100 (intentional); 65 `dp_annex_war` occurrences confirmed.

4. Guard options C and E in ztr_decolonization.4 against pre-existing target country
- Completed: added `NOT = { exists = c:MOR }` trigger to Morocco event's Option C (Create as Colony) and Option E (War of Independence). Prevents the colony pact from converting a pre-existing independent or subject Morocco into ROOT's colony, and prevents `dp_annex_war` from annexing pre-existing Moroccan territory beyond the rebel states. When c:MOR already exists, the player still has Option A (Grant Independence) which safely merges rebel states into the existing country via `ztr_define_morocco`.
- Updated: `events/decolonization/ztr_decolonization.txt`
- Verified: triggers for options C/E in event .4 now block execution if c:MOR already exists; other options unchanged.

## 2026-04-17
2. Decolonization event image selection pass
- Completed: replaced the generic `southamerica_war_civilians` image on `ztr_decolonization.2` through `ztr_decolonization.66` with phase-sensitive `event_image` blocks chosen from vanilla `gfx/media_aliases`, using regional sets for Africa, the Middle East, and Asia plus special cases for Algeria, Persia, India, Japan, China, Siam, and New Zealand.
- Updated: `events/decolonization/ztr_decolonization.txt`
- Verified: events `2-66` now use triggered image aliases, event `67` still keeps the original generic image by design, and the remaining `southamerica_war_civilians` references are the untouched event `67` and event `100` fallback media logic.

## 2026-04-18
5. Subject-independence event overhaul (ztr_decolonization.100 & .101)
- Completed: rewrote `ztr_decolonization.100` with three options — peaceful independence with homeland cession (Option A), negotiated transition keeping the subject in the fold while surrendering homeland states (Option C, phase 3 only), and colonial war (Option B, improved with `ztr_colonial_war_mobilization_modifier` and homeland claims granted to the subject). Added a triggered alternate description (`d_homeland`) when the overlord still owns subject-homeland states. Updated `ztr_decolonization.101` so UN compliance also transfers the subject's homeland states. Added scripted effects `ztr_transfer_subject_homeland_states_effect` and `ztr_claim_subject_homeland_states_effect`, two new modifiers (`ztr_colonial_war_mobilization_modifier`, `ztr_negotiated_transition_modifier`), a demographic boom on liberated subjects, and two new tooltip locv keys (`ztr_decolonization_homeland_cession_tt`, `ztr_decolonization_homeland_claims_tt`).
- Updated: `events/decolonization/ztr_decolonization.txt`, `common/scripted_effects/ztr_decolonization_script.txt`, `common/static_modifiers/ztr_flavor_modifiers.txt`, `localization/english/ztr_decolonization_events_l_english.yml`, `localization/english/ztr_other_l_english.yml`
- Verified: `.100` and `.101` both alias the chosen subject into `scope:subject_scope` before calling the homeland-transfer effect; option triggers gate the homeland-cession path on `ztr_decolonization_has_homeland_states`; `army_size` comparisons use the `{ value = ... }` script-value form; new modifiers use only existing vanilla modifier types; event variables are cleaned up in all option branches.

6. Analyzed UN AI votation cooldown behavior
- Completed: traced the AI UN governance-vote flow from the yearly coordinator JE into proposal selection, vote start, monthly vote countdown, and cleanup. Confirmed the post-vote cooldown is a single global timed variable `ztr_un_vote_cd` set for 4 years after vote resolution, while the active vote itself uses a separate `ztr_un_vote_months_remaining = 12` session timer plus a 30-day launch event delay.
- Updated: `.metadata/LOG.md`
- Verified: `ztr_un_vote_cd` is only referenced in `common/journal_entries/ztr_je_un.txt`, so it gates only the yearly AI proposal coordinator and is not checked by the player proposal buttons or proposal events; AI voting behavior inside an active session is instead driven monthly by `ztr_un_vote_update_neutral_ai_positions` and the threshold formula based on `ztr_un_vote_months_remaining`.

7. Commented state_want_to_be_decolonized checks
- Completed: added inline `#` comments to the shared `state_want_to_be_decolonized` scripted trigger so each gate explains why the state is excluded, why homeland cultures must exist, and why incorporated versus unincorporated states use different acceptance checks.
- Updated: `common/scripted_triggers/ztr_decolonization_triggers.txt`, `.metadata/LOG.md`
- Verified: the trigger logic is unchanged; only explanatory comments were added around the existing checks.

8. Cache colonial-state lookups via ztr_state_is_colonial
- Completed: introduced `ztr_state_is_colonial` scripted trigger in `ztr_decolonization_triggers.txt` that performs a fast `is_target_in_variable_list` lookup against `ztr_colonial_states_list` (the list already rebuilt each yearly pulse by `je_decolonization`). The trigger uses `prev` rather than `root` so it works from any country-scoped state iteration, not just when the colonial power is root. Replaced all `state_want_to_be_decolonized = yes` checks in `ztr_decolonization_phases.txt`, `ztr_decolonization_slavery.txt`, and `ztr_decolonization_script.txt` with the cached membership check. The heavy original trigger still runs once per year inside `je_decolonization`'s list-builder, so dozens of per-event evaluations (region iteration, culture filter loops, homeland checks) collapse into cheap scope-list lookups.
- Updated: `common/scripted_triggers/ztr_decolonization_triggers.txt`, `events/decolonization/ztr_decolonization_phases.txt`, `events/decolonization/ztr_decolonization_slavery.txt`, `common/scripted_effects/ztr_decolonization_script.txt`, `.metadata/LOG.md`
- Verified: the original `state_want_to_be_decolonized` is still used in three intentional places — the JE's list-builder itself, the `is_colonial_power` scripted trigger (which may be evaluated before the list exists), and `colony_pop.txt` (which fires 1880-1936, before the JE is active). All touched events fire from the JE's `random_events` block on the same in-game day the list is rebuilt, so staleness is not a concern.

9. Slight AI lean toward establishing UN institutions
- Completed: added a flat `+5` objective-score bonus inside `ztr_un_value_law` to each of the four "Established" (law_considered = 1) branches for subjects 8–11 (IMF, WTO, ICC, UNESCO). The bonus is added alongside existing ideology/economy/development modifiers, nudging AI countries marginally toward accepting institution creation without overriding strong opposition from command-economy/radical-leftist/traditionalist contexts.
- Updated: `common/scripted_effects/ztr_un_law_opinion_functions.txt`
- Verified: the bonus is applied only inside the limit block that matches each institution's "Established" proposal; the "Not Established" (law_considered = 0) branches are untouched, so opposition logic is preserved.

## 2026-04-19
10. Peaceful Unification diplomatic action
- Completed: implemented `ztr_unify_countries`, a neoimperialism-gated diplomatic action that lets a larger, more prestigious country absorb a small statelet peacefully. Added the master trigger `ztr_countries_can_unify_trigger` plus helpers (`ztr_country_is_statelet`, `ztr_countries_share_primary_culture`, `ztr_countries_state_contiguity`, `ztr_both_democratic`, `ztr_countries_have_cultural_or_relations_ground`, and relations helpers `ztr_unify_actor/target_has_*_opinion`). Rigid gates enforce: actor outranks target and outprestiges them, target is insignificant/minor power statelet, target is not a major power, actor and target share a primary culture OR share religion with amicable relations OR have friendly relations, liberty/equality spectra are not opposite, and an actor homeland state borders or overlaps a target homeland state. Accept effect peacefully transfers every target state to the actor (the engine dissolves the now-stateless target automatically) and applies the new `ztr_unified_country_integration` timed modifier for 10 years. AI acceptance starts at -300 and scales with shared culture/religion/liberty index/relations/attitude/power bloc/rank gap, with infamy and belligerent attitudes being hard blockers.
- Updated: `common/diplomatic_actions/ztr_unify_countries.txt`, `common/scripted_triggers/ztr_main_triggers.txt`, `common/static_modifiers/ztr_flavor_modifiers.txt`, `localization/english/ztr_other_l_english.yml`, `.metadata/LOG.md`
- Verified: action is unlocked by the `neoimperialism` tech (same gate as `ztr_merge_power_bloc_diplomacy`); potential/possible blocks block decentralized, revolutionary, secessionist, and subject countries on either side; relations floor requires at least amicable; infamy above notorious blocks proposal; AI `will_propose` uses `would_accept_diplomatic_action` to avoid spam; linter warnings on inline `"relations(scope:*)"` are false positives (same syntax used in vanilla `54_subjects_investment_rights.txt` and `ztr_invite_to_power_bloc.txt`).

11. European Union power bloc coat of arms
- Completed: added `eu_power_bloc_1` template to `common/coat_of_arms/coat_of_arms/ztr_power_bloc.txt` — solid blue shield (`pb_shield_pattern_00.dds`), yellow circle of stars center (`pb_stars_circle_13.dds`), `pb_top_05`, `pb_frame_00`, and mirrored `pb_side_05` sides — matching the EU flag palette. Registered a new `special_selection` entry in `common/coat_of_arms/template_lists/ztr_coa_templates.txt` that picks `eu_power_bloc_1` at weight 90000 whenever `ROOT.power_bloc_leader` has the `ztr_eu_founder` variable (set by the EU founding decision in `ztr_geopolitic_decisions.txt`).
- Updated: `common/coat_of_arms/coat_of_arms/ztr_power_bloc.txt`, `common/coat_of_arms/template_lists/ztr_coa_templates.txt`, `.metadata/LOG.md`
- Verified: the new template uses only vanilla power-bloc emblem textures confirmed to exist in `gfx/coat_of_arms/colored_emblems/`; the selection trigger reuses the same `ROOT.power_bloc_leader` pattern as the existing GBR/AUS/PRU/RUS/TUR/POR entries, with `?=` guard on the variable check to avoid log spam when the leader scope is missing.

12. Power bloc diplomacy: Superpower & EU adjustments
- Completed: in `ztr_invite_to_power_bloc.txt` and `ztr_join_power_bloc.txt`, replaced the flat `leverage_threshold_to_invite` gate with a `trigger_if/trigger_else` that lowers the required leverage advantage to `50` when the power bloc leader has the `ztr_superpower` modifier (vanilla threshold otherwise). Added a `custom_tooltip` block to both `possible` sections that ties EU power bloc membership to the `ztr_eu_member` modifier: invite is only legal if either (a) the actor leads the EU (`has_variable = ztr_eu_founder`) and the target has `ztr_eu_member`, or (b) the actor is not the EU founder and the target does not have `ztr_eu_member`; symmetric rules for join. Added new on_action `eu_membership_enforcement_on_monthly_pulse_country` (registered in `on_monthly_pulse_country`) that forces `ztr_eu_member` countries into the EU founder's bloc and ejects non-members caught inside it. Added two new locv keys `ZTR_EU_INVITE_RESTRICTION` and `ZTR_EU_JOIN_RESTRICTION`.
- Updated: `common/diplomatic_actions/ztr_invite_to_power_bloc.txt`, `common/diplomatic_actions/ztr_join_power_bloc.txt`, `common/on_actions/ztr_on_actions.txt`, `localization/english/ztr_other_l_english.yml`, `.metadata/LOG.md`
- Verified: `power_bloc_leader = { ... }` scope change is used to inspect the leader from inside the `power_bloc` scope (matches vanilla `00_player_objectives_great_game.txt`); EU enforcement uses `random_country` to find the unique EU founder leader, with `exists` guards before `join_power_bloc`; the leverage threshold branch keeps the `NOT = { any_power_bloc_member = { is_diplomatic_play_enemy_of = ... } }` clause unchanged.

## 2026-04-20
13. Modern Individualism ideology and paradigm-shift event
- Completed: added `ideology_modern_individualism` to `common/ideologies/ztr_ideologies.txt` — a post-Keynesian/social-liberal update of the vanilla `ideology_individualist` that keeps the pro-market stance (laissez-faire, free trade, open migration, private schools/hospitals) but flips the welfare and labor stances to `strongly_approve` old-age pensions, `approve` wage subsidies and public health insurance, and `approve` the right to associate, while still opposing guilds, factory councils and command economy. Created `events/ztr_modern_individualism.txt` with `ztr_modern_individualism.1`, a once-per-country event gated on `country_rank >= rank_value:major_power`, `literacy_rate >= 0.7`, `has_technology_researched = keynesian_economics`, and `ig:ig_industrialists` actually holding `ideology_individualist`; it sets the `ztr_modern_individualism_shift_applied` variable in `immediate`, swaps `ideology_individualist → ideology_modern_individualism` on the industrialists IG, and in the default option calls `start_enactment = law_type:law_old_age_pension` (guarded by `enacting_any_law = no` and no existing pension law). Registered the event via a new `modern_individualism_on_yearly_pulse_country` block in `common/on_actions/ztr_on_actions.txt`. Added localization entries for the ideology (name + consumer-society lore description) in `ztr_ideologies_l_english.yml` and for the event (title "The Birth of the Consumer", long Keynes/consumer-society description, flavor line, and two option strings) in `ztr_events_l_english.yml`.
- Updated: `common/ideologies/ztr_ideologies.txt`, `events/ztr_modern_individualism.txt`, `common/on_actions/ztr_on_actions.txt`, `localization/english/ztr_ideologies_l_english.yml`, `localization/english/ztr_events_l_english.yml`, `.metadata/LOG.md`
- Verified: the ideology reuses the vanilla `gfx/interface/icons/ideology_icons/individualist.dds` icon so no missing-texture warning will appear; `remove_ideology`/`add_ideology` syntax matches the vanilla `00_industrialists.txt` usage (bare `ideology_X` form, no `ideology:` prefix); `has_ideology = ideology:ideology_individualist` in the trigger matches vanilla usage in `major_famine_events.txt` and `india_railway.txt`; `start_enactment = law_type:law_old_age_pension` matches the vanilla `law_imposition_events.1` pattern and is guarded so it cannot interrupt an ongoing enactment or activate on a country that already has the pension law.