ztr_remove_any_power_bloc = {
	set_country_type = unrecognized
	set_country_type = recognized
}

# This chooses a law from the superpower that is giving aid to the country
ztr_select_superpower_alignment_law = {
	if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_governance_principles ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_governance_principles ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
	else_if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_distribution_of_power ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_distribution_of_power ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
	else_if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_citizenship ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_citizenship ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
	else_if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_church_and_state ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_church_and_state ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
	else_if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_internal_security ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_internal_security ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
	else_if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_bureaucracy ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_bureaucracy ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
	else_if = {
		limit = {
			scope:superpower_law_requester = {
				active_law:lawgroup_army_model ?= {
					law_is_available = yes
					ROOT = {
						NOT = {
							has_law_or_variant = prev.type
						}
					}
				}
			}
		}
		scope:superpower_law_requester = {
			active_law:lawgroup_army_model ?= {
				save_scope_as = superpower_alignment_law
			}
		}
	}
}

join_eu_law_changes = {
	# Universal suffrage
	if = {
		limit = {
			NOT = { has_law = law_type:law_universal_suffrage }
		}
		activate_law = law_type:law_universal_suffrage
	}
	# Parliamentary republic
	if = {
		limit = {
			OR = {
				has_law = law_type:law_corporate_state
				has_law = law_type:law_theocracy
			}
		}
		activate_law = law_type:law_parliamentary_republic
	}
	# Cultural exclusion
	if = {
		limit = {
			NOR = {
				has_law = law_type:law_multicultural
				has_law = law_type:law_cultural_exclusion
			}
		}
		activate_law = law_type:law_cultural_exclusion
	}
	# Compulsory primary school
	if = {
		limit = {
			NOT = {
				has_law = law_type:law_compulsory_primary_school
			}
		}
		activate_law = law_type:law_compulsory_primary_school
	}
	# Women's suffrage
	if = {
		limit = {
			NOT = {
				has_law = law_type:law_womens_suffrage
			}
		}
		activate_law = law_type:law_womens_suffrage
	}
	# Interventionism
	if = {
		limit = {
			NOR = {
				has_law = law_type:law_interventionism
				has_law = law_type:law_laissez_faire
			}
		}
		activate_law = law_type:law_interventionism
	}
	# Protectionism
	if = {
		limit = {
			NOR = {
				has_law = law_type:law_mercantilism
				has_law = law_type:law_isolationism
			}
		}
		activate_law = law_type:law_protectionism
	}
	# Protected speech
	if = {
		limit = {
			NOT = {
				has_law = law_type:law_protected_speech
			}
		}
		activate_law = law_type:law_protected_speech
	}
}