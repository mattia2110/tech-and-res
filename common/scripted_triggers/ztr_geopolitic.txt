# Triggers

# Superpower Alignment Trigger

ztr_has_superpower_alignment_law_candidate = {
	OR = {
		active_law:lawgroup_governance_principles ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
		active_law:lawgroup_distribution_of_power ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
		active_law:lawgroup_citizenship ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
		active_law:lawgroup_church_and_state ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
		active_law:lawgroup_internal_security ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
		active_law:lawgroup_bureaucracy ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
		active_law:lawgroup_army_model ?= {
			law_is_available = yes
			ROOT = {
				NOT = {
					has_law = prev.type
				}
			}
		}
	}
}
